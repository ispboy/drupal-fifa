{#
/**
 * @file
 * Default theme implementation to present a match entity.
 *
 * This template is used when viewing a registered match's page,
 * e.g., /admin/content/fifa-match)/123. 123 being the match's ID.
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 *   print a subset such as 'content.title'.
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_fifa_match()
 */
#}
<article{{ attributes }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</article>
