{#
/**
 * @file
 * Default theme implementation to present a team entity.
 *
 * This template is used when viewing a registered team's page,
 * e.g., /admin/content/fifa-team)/123. 123 being the team's ID.
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 *   print a subset such as 'content.title'.
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_fifa_team()
 */
#}
<article{{ attributes }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</article>
